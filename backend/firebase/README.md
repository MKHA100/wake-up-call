# Firebase Backend Skeleton

This folder contains the initial backend contract skeleton:
- `firestore.rules`: user-scoped reads/writes and immutable penalties.
- `functions/src/index.ts`: callable function placeholders.

## Next Integration Steps
1. Configure Firebase project and service accounts.
2. Add Firestore schema migration scripts.
3. Implement function business logic and validation.
4. Add emulator tests for auth/rules/functions.
